export const pathDefaultProps = {
  strokeColor: '#ff0000',
  strokeWidth: 2,
  strokeScaling: false,
}

export const circleDefaultProps = {
  radius: 5,
  fillColor: '#fff',
  strokeColor: '#000',
  strokeWidth: 1,
  strokeScaling: false,
}

export const anchorDefaultProps = {
  radius: 5,
  fillColor: '#fff',
  strokeColor: '#000',
  strokeWidth: 1,
  strokeScaling: false,
}
